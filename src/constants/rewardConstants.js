export const REWARD_RULES = {
  FIRST_THRESHOLD: 50,
  SECOND_THRESHOLD: 100,
  FIRST_MULTIPLIER: 1,
  SECOND_MULTIPLIER: 2,
};

export const MONTH_FORMAT = 'default';

export const DATE_OPTIONS = {
  month: 'long',
};